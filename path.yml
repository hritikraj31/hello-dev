version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
env:
  variables:
    "testEnv" : "testValue1"

steps:
  - type: Command
    name: "Build Source"
    timeoutInSeconds: 400
    command: |
      echo $PATH
      PATH=PATH=/abc/bcd:$PATH
 - type: Command
    name: "Build Source"
    timeoutInSeconds: 400
    command: |
      echo $PATH
