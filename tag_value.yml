version: 0.1
component: build
timeoutInSeconds: 6000

env:
  exportedVariables:
    - tagValue


steps:
  - type: Command
    name: "Clone private repo"
    timeoutInSeconds: 400
    command: |
      tagValue=`git tag --points-at HEAD`
      echo $tagValue
